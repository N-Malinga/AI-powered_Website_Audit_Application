# Frontend (placeholder)

React UI for the Website Audit Tool. Scaffolded in a later phase.

Per `CLAUDE.md`, the UI keeps **Metrics**, **Insights**, and **Recommendations** visually
separate, and consumes the backend `POST /api/audit` endpoint.
