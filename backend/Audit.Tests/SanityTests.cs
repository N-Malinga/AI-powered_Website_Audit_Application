namespace Audit.Tests;

/// <summary>
/// Placeholder test so the project references compile and `dotnet test` runs green.
/// Replaced by real metrics-extraction tests in the logic phase.
/// </summary>
public class SanityTests
{
    [Fact]
    public void Solution_builds_and_tests_run()
    {
        Assert.True(true);
    }
}
